services:
  web:
    build: .
    container_name: recipe_bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    command: gunicorn --bind 0.0.0.0:8080 --workers 2 --timeout 120 wsgi:app
